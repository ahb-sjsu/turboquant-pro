# 100M SMOKE. Compute the seeded query set once (4 shards x 25 queries = 100)
# so the reference and IVF jobs load the cache instead of regenerating blocks.
apiVersion: batch/v1
kind: Job
metadata:
  name: aqx-qcache100m
  namespace: ssu-atlas-ai
  labels: {app: tqp-fleet, "atlas.io/batch": tqp-100b}
spec:
  backoffLimit: 0
  ttlSecondsAfterFinished: 3600
  template:
    metadata:
      labels: {app: tqp-fleet, "atlas.io/batch": tqp-100b}
    spec:
      restartPolicy: Never
      containers:
      - name: qcache
        image: python:3.12
        command: ["/bin/bash", "-lc"]
        args:
        - |
          set -euo pipefail
          export PIP_ROOT_USER_ACTION=ignore PYTHONUNBUFFERED=1
          pip install -q --no-cache-dir numpy
          export PYTHONPATH=/work
          python /work/fleet_qcache.py
        env:
        - {name: TQP_QCACHE_NAME, value: "queries100m.npy"}
        - {name: TQP_QUERY_SHARDS, value: "0,5,10,15"}
        - {name: TQP_QUERIES_PER_SHARD, value: "25"}
        resources:
          requests: {cpu: "4", memory: 8Gi, ephemeral-storage: 2Gi}
          limits: {cpu: "4", memory: 8Gi, ephemeral-storage: 2Gi}
        volumeMounts:
        - {name: shared, mountPath: /shared}
        - {name: code, mountPath: /work}
      volumes:
      - name: shared
        persistentVolumeClaim: {claimName: tqp-fleet-shared}
      - name: code
        configMap: {name: tqp-fleet-code}
